pipeline {
    agent any

    stages {
        stage('dev') {
            steps {
                echo 'dev environment'
            }
        }
        stage('test') {
            steps {
                echo 'test environment'
            }
        }
    }
}
